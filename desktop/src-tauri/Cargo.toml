[package]
name = "stocks-desktop"
version = "0.1.0"
description = "Stocks IDX standalone desktop app"
authors = ["Mqdd27"]
edition = "2021"

[build-dependencies]
tauri-build = "2"

[dependencies]
argon2 = "0.5"
keyring = "3"
rand = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }